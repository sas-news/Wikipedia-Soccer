# Render Blueprint — https://render.com/docs/blueprint-spec
# 「New Blueprint Instance」でこのリポジトリを指定するだけでサービスが作られます。
services:
  - type: web
    name: wikipedia-soccer
    runtime: node
    plan: free
    region: singapore
    branch: main
    buildCommand: npm ci && npm run build && (if [ -f data/difficulty.db.gz ]; then gzip -dkf data/difficulty.db.gz; fi)
    startCommand: npm run start
    healthCheckPath: /healthz
    envVars:
      - key: NODE_VERSION
        value: "20"
